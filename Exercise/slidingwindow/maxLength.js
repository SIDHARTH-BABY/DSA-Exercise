function maxLength(tar, nums) {
    let sum = 0
    for(let i=0;i<nums.length;i++){
 if(sum === tar){
    
 }
    }
}

let target = 7;
let nums = [2, 3, 1, 2, 4, 3];
console.log(maxLength(target, nums));
